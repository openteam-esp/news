class Migrator::CopyFile
  def migrate
    #Ckeditor::Asset.all.each do | file |
      #file.data = File.new("#{Rails.root}/public/files/#{file.old_id}/original/#{file.data_file_name}")
      #file.data.save
    #end
  end
end

