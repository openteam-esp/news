Вы уверены что хотите переместить новость в корзину?
<%= composed_title @entry %>

<%= semantic_form_for @entry, :html => { :method => :delete } do | form |  %>
  <%= form.actions :class => "buttons" do  %>
    <%= form.action :submit %>
    <li><%= link_to t('cancel'), [:manage, :news, @entry] %></li>
  <% end %>
<% end %>
