<!DOCTYPE html>
<html lang="ru-RU">
  <head>
    <title><%= t("News system") %></title>
    <meta charset="UTF-8">
    <%= csrf_meta_tag %>
    <%= favicon_link_tag %>
    <%= stylesheet_link_tag "screen" %>
    <%= javascript_include_tag "jquery.min" %>
    <%= yield :extend_javascript %>
    <%= javascript_include_tag "jquery.ujs" %>
    <%= javascript_include_tag "application" %>
    <!--[if lte IE 7]>
    <%= stylesheet_link_tag "ie" %>
    <![endif]-->
  </head>
  <body>
    <% flash.each do |name, msg| %>
      <div id="flash_wrapper">
        <%= content_tag :div, msg, :id => "flash_#{name}", :class => "flash_block" %>
      </div>
    <% end %>
    <div class="header">
      <div class="login_info">
        <% if current_user %>
          <%= current_user.name %>
          <%= "[#{current_user.roles.join(', ')}]" if current_user.roles %>
          <%= link_to t("logout"), destroy_user_session_path, :method => :delete %>
        <% end %>
        <%= link_to t("login"), new_user_session_path unless current_user %>
      </div>
      <h1><%= link_to t("News system"), root_path %></h1>
      <%= semantic_form_for :entry, :url => entries_path do | form | %>
        <%= form.buttons do %>
          <%= form.commit_button t('new_entry'), :button_html => { :class => "button" } %>
        <% end %>
      <% end %>
    </div>
    <div class="container">
      <div class="sidebar">
        <div class="notifications"><%= link_to(t("sidebar.notifications"), "#") %></a></div>
        <div class="tasks">
          <h1><%= t("sidebar.tasks.title") %></h1>
          <%= render_navigation :context => :tasks, :renderer => :list_with_counters %>
        </div>
        <div class="enties">
          <h1><%= t("sidebar.entries.title") %></h1>
          <%= render_navigation :context => :entries, :renderer => :list_with_counters %>
        </div>
        <div class="archive">
          <h1><%= t("sidebar.archive.title") %></h1>
          <ul>
            <li><%= link_to(t("sidebar.archive.last_day"), "#") %></li>
            <li><%= link_to(t("sidebar.archive.last_week"), "#") %></li>
            <li><%= link_to(t("sidebar.archive.last_month"), "#") %></li>
            <li><%= link_to(t("sidebar.archive.search"), "#t") %></li>
          </ul>
        </div>
      </div>
      <div class='content'>
        <%= yield %>
        <ul class="entry_filter">
          <li class="selected"><a href="#">Все</a></li>
          <li><a href="#">Корректура</a></li>
          <li><a href="#">Публикация</a></li>
        </ul>
      </div>
    </div>
    <div class="footer">
      <p class="float_right">
        Сделано с любовью в <a target="_blank" href="http://www.openteam.ru">OpenTeam</a>.
        Если случилась беда &mdash; <a target="_blank" href="http://tasks.openteam.ru/projects/esp-news/tasks/new">сообщите об ошибке</a>
      </p>
      <p class="float_left">Система публикации новостей, версия 1.0</p>
    </div>
  </body>
</html>

