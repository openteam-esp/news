<!DOCTYPE html>
<html lang="ru-RU">
  <head>
    <title><%= t("News system") %></title>
    <meta charset="UTF-8">
    <%= csrf_meta_tag %>
    <%= stylesheet_link_tag "screen" %>
    <%= javascript_include_tag "jquery.min" %>
    <%= yield :extend_javascript %>
    <%= javascript_include_tag "jquery.ujs" %>
    <%= javascript_include_tag "application" %>
  </head>
  <body>
    <% flash.each do |name, msg| %>
      <div id="flash_wrapper">
        <%= content_tag :div, msg, :id => "flash_#{name}", :class => "flash_block" %>
      </div>
    <% end %>
    <div class='header'>
      <h1><%= link_to t("News system"), root_path %></h1>
      <div class="login_info">
        <% if current_user %>
          <%= current_user.name %>
          <%= "[#{current_user.roles.join(', ')}]" if current_user.roles %>
          <%= link_to t('logout'), destroy_user_session_path, :method => :delete %>
        <% end %>
        <%= link_to t('login'), new_user_session_path unless current_user %>
      </div>
    </div>
    <div class='container'>
      <div class='sidebar'>
        <%= link_to t('new_entry'), new_folder_entry_path(Folder.where(:title => 'draft').first), :class => 'new_entry' %>
        <%= render_navigation :context => :folders if current_user %>
        <%= render_navigation :context => :channels %>
      </div>
      <div class='content_wrapper'>
        <%= yield %>
      </div>
    </div>
    <div class="footer">
      <p>Система публикации новостей, версия 1.0</p>
      <p>
        Сделано с любовью в <a target="_blank" href="http://www.openteam.ru">OpenTeam</a>.
        Если случилась беда &mdash; сообщите об <a target="_blank" href="http://issues.openteam.ru/projects/show/ato-news">ошибке</a>
      </p>
    </div>
  </body>
</html>

