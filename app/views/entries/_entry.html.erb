<%= presented_header entry %>
<%= presented_html entry, :annotation %>
<%= presented_html entry, :body %>

<%= render_attribute_values entry, :images, :videos, :audios, :attachments %>
<hr />

<%= render :partial => 'entries/availability', :locals => { :entry => entry }  %>

<%= render_attribute_values entry, :channels, :events %>

